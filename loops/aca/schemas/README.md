Drop the real IRS AIR `.xsd` files here (flat, no subfolders). See
`../schema_notes.md` for which files and where to get them. This folder is
empty in the shipped tool on purpose -- see that file for why.
