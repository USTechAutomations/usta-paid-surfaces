# Current monthly access — September 10, 2026

QRelay, LedgerMatch and CasePack now use `loop_subscription_authorities` in the existing Firestore database. `/pro/claim` binds the original paid live Checkout to its exact monthly product, price, customer and subscription. `/pro/verify` and paid tool operations re-read current subscription, latest paid invoice, invoice line period, succeeded PaymentIntent and expanded captured charge. Taxes are supported; discounts, credits, proration, partial capture, refunds and disputes do not grant these fixed-price contracts. Known nonpaying states return 403; unavailable evidence returns retryable 503. Cancellation at period end retains the proved paid term. A later paid renewal restores the same key without deleting its original binding or paid configuration history. Manual revocations remain effective.

No raw Checkout identifier or paid key is stored in the authority document or public source. Recovery still uses the existing private Stripe return URL. Keys issued without a monthly authority must be recovered once from their original checkout; an unbound key returns UNKNOWN rather than unlimited access. The fresh full Checkout reads in `stripe-before.json` found no existing sessions on these three links at the recorded time, so no customer migration was fabricated. SchemaHand's separate payment authority and private artifact delivery are preserved. ACACheck is off sale.

Deployment: `loops/deploy.sh --payment-recovery --monthly-access --expected-app-sha256 <fresh hash> --expected-image <fresh serving digest>`. The seven-file overlay requires the actually inspected immutable base image and rechecks the serving revision before replacing traffic. Current local app sources were older than the serving accessibility/brand changes; those serving changes were preserved and synchronized before release. Never publish the older app files from a checkout merely because its main app hash matches. Shared CSS and unrelated source changes were not replaced.

Release evidence: `/home/gmullins/reports/customer-path-execution-20260910/monthly-published.json` records revision usta-loops-00010-wb7, 100% traffic and 12 matching source/dependency files, including unchanged SchemaHand authority, private delivery, stores and served template. `/health` returned 200 with Firestore. `monthly-public-acceptance.json` records 24 passed hosted probes: malformed/nonexistent purchase refusal and synthetic free CasePack/QRelay/LedgerMatch create-output-delete paths. Every created artifact was deleted and its former read URL returned 404. These are internal synthetic events, not demand or paid production acceptance. No real purchase or synthetic revenue was created.

Tests: existing manual service suite 332 assertions passed; the final focused monthly suite has 26 tests including the direct scheduler CLI/import/pagination regression; hosted apps have 134 passing tests. Service unittest discovery passed before that final scheduler addition with 101 passes and 3 explicit emulator skips. An attempted local AMD64 container run was UNKNOWN (`exec format error` on this ARM host); actual hosted health and functional readbacks above are separate evidence.

The existing annual-expiry job now imports `fv5.lib.stripe_read` unambiguously and handles pagination without treating malformed pages as an empty success. It no longer permanently revokes monthly access on temporary subscription failures. The actual scheduled Python executable's dry run returned 0 and found no paid sessions to revoke on the four current software links. This job remains a fixed-term annual fallback; it is not the monthly access authority.

The existing canonical `stripe_revenue_sync.py --scope monthly` now accounts for QRelay, LedgerMatch and CasePack through `invoice.paid`, a pinned invoice read, the exact original Checkout/price/subscription, and captured PaymentIntent/Charge proof. It stores gross cash through the existing `business_metrics.db::revenue_events` writer; access and delivery never create cash. Retries deduplicate the PaymentIntent, and ambiguous identity, tax, pagination or schema retains the monthly cursor with UNKNOWN. Initial subscription Checkout normally has no PaymentIntent: never invent one or use the invoice summary period instead of its service line.

The installed accounting suites passed 99 tests. `monthly-accounting-installed.json` records the actual existing service exiting 0 on 2026-09-10 at 18:46 UTC: three contracts verified, 33 events read, zero selected monthly captures, and all 17 preexisting cash rows preserved. The existing 30-minute service sequence retains SchemaHand and accepted-order reconciliation before monthly accounting. No new timer was created. `loops/facts.py` reads only the reviewed scoped receipts; `loops/metrics.py` renders the three-family gross counts while keeping paid investment fields UNKNOWN. Its 32 tests passed. Historic coverage, refunds/net cash, acquisition attribution and customer acceptance remain separate evidence gaps.

---

The following is the historical September 9 release record, superseded above where monthly behavior differs.

# Customer delivery repairs — September 9, 2026

Scope: five existing tools (CasePack, LedgerMatch, QRelay, SchemaHand, ACACheck), their existing payment links, and the existing `usta-loops` service. No new catalog, service, database, signing key, paid run or customer message was created.

The five `/feeds/<family>/p/thanks/` pages retrieve a key with `POST /pro/claim`. The service reads Stripe using the existing restricted credential, checks the exact product/payment link, paid completed live checkout, current paid subscription or one-payment term, successful charge, refund/dispute state and existing revocation store. A failed lookup gives an unavailable/retry state. Keys remain in the buyer's response/browser; they are never generated into these static pages. FV5 skips these five static-key fulfillment modules after this cutover. Keep the purchase-return URL private; possession is the recovery credential. The signing format remains compatible with existing product verification.

The existing CasePack external-embed repair is included: public configuration GET and its constrained GET preflight allow other origins. Mutations still need their existing secrets and retain origin restrictions. No customer configuration was created to test this change.

Validation: 21 offline payment-to-key-to-product acceptance cases, 286 service/store assertions, FV5's 40 assertions, and direct-key bypass checks. Browser return-page acceptance used a synthetic response, not a real payment. Direct Stripe pagination found no checkout sessions on these five payment links on 2026-09-10 UTC. Refund denial is checked during recovery; the legacy revocation scheduler still needs a reviewed refund/renewal lifecycle extension for previously issued keys. A successful paid production purchase and retained customer value are unestablished.

Deployment uses the existing service and the currently serving immutable image. `loops/deploy.sh --payment-recovery --expected-app-sha256 <freshly-extracted-app-hash>` copies only the reviewed service files, checks the current revision again and preserves existing configuration. It builds locally; it does not invoke Cloud Build. The service preserves its existing Secret Manager signing binding. Its Stripe read credential is currently a private Cloud Run environment value supplied from the authorized existing reader; it is never copied into an image or printed. Do not dump complete service configuration to reports. Future default deployments update rather than replace environment/secret settings.

The static release used existing overlay build/deploy functions and the current feeds image, preserving concurrent directory/search changes and all other routes and sitemap bytes. The changed pages use `styles.css?v=customer20260909`; the unversioned edge cache can retain older CSS. Only the two already documented accessible button-surface tokens were added to each shared theme definition. Scoped pages reference those tokens. Strict brand checks pass on the eight changed pages; candidate light/dark, six-width checks and published content hashes are in the evidence folder. Performance and estate-wide accessibility remain unmeasured.

`loops/metrics.py` no longer calls delivery rows payments or turns unavailable events into zero/kill verdicts. Per-product canonical receipt attribution remains UNKNOWN. Observed service events include synthetic/internal activity and are not qualified demand. `loops/ledger.py` grants zero spending authority; revenue and a guessed budget cannot authorize paid model calls under the operator's policy.

Evidence: `/home/gmullins/reports/customer-journeys-20260909/`. Backend receipt: `loops-after.json`. Published bytes: `published-after.json`, `published-css-font.json`, `font-after.json`. Render checks: `browser-return-page-check.json`, `browser-store-pages.json`. Independent weekly-feed correction repair: `/home/gmullins/code/wt-delivery-corrections-20260909/docs/DELIVERY_CORRECTIONS.md`.

The follow-up release updates the five existing software sales pages to describe the same private Stripe-return key retrieval. It changes no prices. The full changed-page set is thirteen pages; all five follow-up published HTML hashes match candidates, the five public page-quality checks have zero blocking findings, and the final browser run passed sixty width/theme observations. Evidence: `offers-published-after.json`, `browser-offer-pages.json`, `offer-pages-deployment.json`.
