{
  "actorOutputSchemaVersion": 1,
  "title": "Output of NRC Spill Notices Scraper",
  "properties": {
    "results": {
      "type": "string",
      "title": "Spill notices",
      "description": "All items the run produced, one per record, as JSON.",
      "template": "{{links.apiDefaultDatasetUrl}}/items"
    },
    "results_csv": {
      "type": "string",
      "title": "Spill notices (CSV)",
      "template": "{{links.apiDefaultDatasetUrl}}/items?format=csv"
    },
    "summary": {
      "type": "string",
      "title": "Source, coverage and run status",
      "description": "Check SUCCESS, NO_MATCHES, UNKNOWN or interrupted output, source hash and omitted-match count.",
      "template": "{{links.apiDefaultKeyValueStoreUrl}}/records/SUMMARY"
    }
  }
}
