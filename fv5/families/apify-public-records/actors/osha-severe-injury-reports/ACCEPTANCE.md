# OSHA source and marketplace acceptance

Observed 12 September 2026. Public default 0.3.2 / b423Snv0cesyjmcFm; promotion and readback HTTP 200 in promotion.json. The canonical public Store page returned 200 at 22:32 UTC, showing the checked-copy description, historical coverage, example, current price, active action and no deprecated warning (public-osha.json).

The previous URL was obsolete. The original parser also compared U.S. dates lexically and full source state names against abbreviations. The official isolated browser fetch returned 200 for the dashboard and linked ZIP. The direct-source candidate's actual hosted run JboVqacqgaM6eeZ7C failed with HTTP 403/raw exit 91 and no rows/events. This was a source-route failure, not proof that OSHA was unavailable everywhere.

The accepted route uses the daily isolated-browser collector and an owned public Apify source copy. Original ZIP SHA-256 a3f7f434e200fb956131f12277378e592993a25db3f328716fbece106f846bb0; sanitized ZIP SHA-256 2f92022df68d48f500a7fa8eef5be3f35bfe7c860d55d4756fd78b4097f960b2. The raw file contains 105,996 rows covering 2015-01-01 through 2025-11-30. These counts come from the downloaded file. Only allowlisted fields enter the public copy; street addresses, narratives, ZIP codes and coordinates are excluded. Distinct UPA reports sharing a displayed ID are preserved.

- Copy and both installed actor SMOKE commands: 35 tests, raw exit 0, actual SDK import. Collector: 9 tests, raw exit 0 on copy and both sources. Logs are in this folder.
- Independent CSV/SQLite oracle matches three New York 2025 records and 418 matching reports across the whole source. A three-item cap omits 415 matches explicitly.
- Actual local SDK with the anonymous public cache returns three rows/raw exit 0. Injected stale clock and corrupted download each fail UNKNOWN/raw exit 91 with no rows; empty-array input fails INVALID_INPUT/raw exit 91. These fault tests are local, not hosted outage observations.
- Actual hosted cache run RoP4xanoJ6VNeQUZo on exact default build: SUCCEEDED/raw exit 0, three source-matching rows and three automatic dataset-item events. JSON and CSV exports HTTP 200. provider-export.json records the CSV identity/date/source comparison.
- Real installed service invocation 9c3f87cff718484cb130110d03db4191: company guard PASS/raw exit 0; collector raw exit 0; artifact and latest pointer anonymous readbacks HTTP 200. Timer is active for 03:40 America/Phoenix daily, Persistent=false. Its first scheduled firing remains UNKNOWN; the manual service invocation is proved.
- Store screenshots at 1280 and 390 pixels show the product and focusable primary link without horizontal overflow. The provider rendered dark despite both requested color schemes. Light mode, full keyboard journey, account signup and an independent paid buyer remain unaccepted.

Current price: $0.005 per returned dataset item; no Actor Start event. All pricing history was preserved. These owner runs used the existing FREE allowance and hard monthly cap; platform usage is separate from result charges, and this does not promise free usage to every buyer.

Runtime limits: 1,000 returned records maximum; full source scan before output; source copy older than 48 hours, wrong hash/schema, unavailable input and timeouts refuse without fabricated empty success. Check both SUMMARY and actual run status; interrupted writes leave OUTPUT_IN_PROGRESS. This historical file is not a complete U.S. injury census or an enforcement finding.

Operation and recovery: see the installed collectors/osha/README.md. Do not use legacy PUBLISH.sh or refresh.py diagnostics as acceptance. Retain pinned Apify SDK 2.7.3/Pydantic 2.11.10/Browserforge 1.2.3 until fresh startup and provider tests justify a change. Reverting to old build dKu661g65aBHwofhk also requires restoring deprecated status because that build is broken; never restore its start charge.

Commercial acceptance remains UNKNOWN: independent demand, payment, repeat use and attribution have not been established. The bounded owner run is delivery evidence, not demand. See GRADE.md, IMPROVEMENTS.md and evidence-manifest.json in /home/gmullins/reports/marketplace-closure-20260912/osha-recovery/.
