# Customer return and extension integration — September 9, 2026

Five existing Stripe-confirmed license key pages remain immediate retrieval routes. Eight file-return pages now use private POST delivery, replacing public-path polling. Both return templates strip and validate the checkout capability before external resources, store it only in family-specific tab storage, and direct lost-access recovery to receipt-based contact. They do not promise a receipt email contains a recovery link. All thirteen use the canonical brand shell and Satoshi, with accessible action states. The generator reads ETA literals via AST instead of executing fulfillment modules.

Root matched source hashes before application and generated all thirteen pages byte-for-byte equal to the frozen candidate. Four installed-source tests passed. Candidate acceptance included thirteen positive flows plus refresh, twelve negative cases,156 responsive conditions,104 contrast checks, strict brand/truth on all thirteen, and explicit loaded Satoshi checks. Production private-store and wrong-buyer refusal tests are separate evidence in the session release folder.

KDP/Grant Fit pages retain their published Chrome installation links while stating the observed store version and unverified Edge status. KDP samples and privacy copy no longer present unsupported sales predictions as accepted outputs. The owned page distinguishes the prepared observation-only0.1.1 update from Chrome’s currently published0.1.0. Five public files and ten catalog fields changed; unrelated dirty catalog fields remain unstaged. The corrected extension ZIP, listing fields, privacy declarations and screenshots require human Chrome submission before any store-update claim.

Release material and exact public readback: `/home/gmullins/advisor-plans/business-integration-20260909/`. No global CSS, main website, payment catalog or sitemap promotion was changed. A COPY-only image overlay preserves the latest owned feeds runtime. Initial HTTP return queries and installed-store parity remain distinct limitations; neither is hidden by local test results.
