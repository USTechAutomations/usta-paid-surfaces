# September11 hardening repairs

Existing code now distinguishes unavailable checkout counters, refuses a missing observation script and replaces the actual script element without duplicating it. Installed regressions cover comments, attributes and ambiguous elements. The freshness monitor bounds response reads and rejects unreadable/future metadata; pause text must be visible. A concurrent follow-up strengthened HTML metadata parsing and remains preserved.

LedgerMatch pairs equal amounts for repeated references before comparing unmatched rows. Reordered exports no longer invent discrepancies. Exact matching-service release evidence is under advisor-plans/same-day-revenue-closure-20260911/execution/enterprise-hardening/invoice-deploy.json, with synthetic hosted output proof adjacent. Locale-ambiguous numbers and signed split suggestions were not changed by this repair.

SchemaHand Python and browser parsers retain named MySQL UNIQUE KEY/INDEX constraints, decimal defaults and doubled backticks. New tests assert independent expected values in both implementations. No price, entitlement or paid allowance changed.

Run the existing tests plus loops.test_checkout_observations_hardening, tests/test_freshness_hardening.py, loops.service.apps.tests.test_duplicate_references, loops.schemahand.tests.test_parser_hardening and loops.schemahand.tests.test_browser_parity. Tests use synthetic input. Relevant exact commands, raw exits, before failures and manager rejections are retained in the shared execution directory. Do not mistake green fixture suites or these source repairs for portfolio paid-readiness or proven demand.

Current company ingress uses gx10-52e1.tail530d80.ts.net. The two API locations are retained by scripts/preserve_independent_overlays.py from the current serving image during full rebuilds. Source/public states and remaining payment configuration are in execution/company-ingress-continuation; do not restore obsolete taild7180f routes.
