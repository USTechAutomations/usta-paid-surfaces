# Two dated packs held from public use

The installed source-use hold covers only `hospital-mrf` and `model-cards`.
Positive source-use permission is unresolved. Do not restore purchases, public
sample rows or chargeable catalog fields from historical drafts. Private sealed
history is preserved; a source-use hold does not authorize new distribution.

`pack_file` returns no public rows without reading the seal, and refuses a stale
chargeable catalog row before rendering. The renderer, hub and coverage builder
show unavailable copy. Both existing mint paths block these two families before
provider access, including when a stale writer restores `TO-MINT`. Catalog rows
carry `source_use_hold`, an unknown sample status, no price and no checkout URL.
Other rows and the current Texas manifest acceptance are preserved.

The four old source sample copies were moved to a private, hash-bound backup at
`/home/gmullins/code/plan023-delete-backup-ig4h8e3b`; the original seals under
`usta-autonomous-packs/var/seals` were not touched. The deletion guard first
refused a generated-date drift; fresh comparison found only JSON `generated`
changes, recorded the new hashes, and then moved the exact allowlisted files.
The existing `build_slices` unknown-sample path also removes stale public copies.

Public release evidence is separate from source installation. Consult
`/home/gmullins/advisor-plans/business-launch-closure-20260910/release/feeds-source-hold-promotion.json`
and its follow-through evidence before claiming a public outcome. The overlay
changes four existing HTML pages plus the exact nginx sample blocks while
inheriting the remaining pinned image. Tests use retained base files with
positive baseline controls, so an absent-file 404 cannot stand in for a block.

Installation receipts and six installed-source regression cases are in
`release/final-source-hold-apply.json` and `release/source-hold-installed-tests.json`
under that analysis directory. Strict truth acceptance uses the fresh installed
catalog and all four actual candidate HTML files. No source-use permission,
customer order, or customer delivery is inferred from these checks.

The serving revision was `usta-feeds-00111-fd7` at the 22:38 UTC readback.
All four public pages returned 200 with their accepted hashes, and all 72
sample aliases returned 404 across the public domain and both service origins.
The original publisher receipt retains the earlier traffic mismatch refusal;
the promotion and follow-through receipts record its subsequent resolution.
Do not erase or relabel the refusal as a successful initial deploy.

The original GitHub Pages pack pages were found to retain old sample tables
after this feeds release. Their independent correction is tracked in
`closure/final-source-hold/github-pages-hold/`; inspect the eventual Pages
Deploy-job and public-byte receipt before calling that surface corrected.
