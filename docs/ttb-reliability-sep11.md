# TTB reliability and measurement, September 11

Acceptance and grade: /home/gmullins/reports/growth-close-20260911/ACCEPTANCE.md and GRADE.md. Required commerce gate passes; canonical ship recorded CHECKPOINT because shared masthead platform-pricing navigation trips the recorder's separate plan-price check. Do not infer SHIPPED from the commerce gate.

The TTB family_spec and sample use the same held-pair data in build_slices; build_wave2 accepts the current sample for this parent. Changed-field-only comparisons render their own table. Missing/unreadable input exits UNKNOWN/2 before replacing accepted outputs. check_site compares every table's identifiers against both sample formats and refuses inconsistent dates/rows. No second refresh writer is needed.

build_site adds TTB-only public-sample Dataset JSON-LD and a checkout click handler. deploy/nginx.conf serves only /click/ttb.gif as image/gif with no-store. click_beacon.js uses existing checkout anchors, honours DNT/GPC and marks automation probe=1. funnel_weekly excludes that marker and failed click requests. This is aggregate event tracking, not individual referral-to-payment attribution.

Stable selftest pins use message signatures rather than source line numbers. Unique temporary directories prevent cross-run contamination. Untouched estate must pass; it is never rewritten to hide a failure. Missing fixture cases fail. Existing 73 mutations pass; five extra guard sites remain explicitly UNKNOWN. The inert Subscribe button detector now refuses a missing destination while allowing non-purchase Copy/status controls.

Portable fake-provider delivery tests: ../usta-autonomous-packs/ttb_tests. Public paid.html was compared byte-for-byte with tested code. No real charge occurred.

Read-only review timers: ttb-growth-review-72h.timer and ttb-growth-review-14d.timer, September 14/25 at09:45MST. Script and evidence: /home/gmullins/reports/growth-close-20260911/review_experiment.py and reviews/. Persisted late activation obeys the10:00–15:00quiet window and reports UNKNOWN without probes if it lands there. Timers can be disabled with systemctl --user disable --now followed by their two names. No provider login, sends, spending or publishing in reviews.

The existing publish_overlays.py exact allowlist now includes click/ttb.gif; negative unrelated destinations still refuse. Immutable-image readback and explicit pinned-traffic promotion were required; see promotion.json. Check current serving head before any later release or rollback.
